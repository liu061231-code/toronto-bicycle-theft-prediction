# R package dependencies for the Toronto bicycle theft prediction project.
#
# This project is written in R (>= 4.2). Install the required packages from
# CRAN with:
#
#   install.packages(scan("requirements.txt", what = "character"))
#
# The list below mirrors `required_packages` in `src/config.R`.

dplyr
tidyr
readr
tibble
lubridate
ggplot2
scales
splines
glmnet
MASS
digest
jsonlite
testthat
