# 给 WorkBuddy 的后续优化交接

日期：2026-09-19  
交接方：Codex  
接手方：WorkBuddy  
项目：Toronto Bicycle Theft Prediction（STAT3888 课程项目延伸）

## 1. 任务目标与交接状态

本文件由用户要求生成，供后续优化升级使用。目标是把现有研究原型改造成**方法可信、可复现、可预测未来月份、适合作为研究生申请作品**的项目。

本次交接仅生成文档，尚未实施下面的修改。请在用户启动后续优化任务后按阶段推进；本文不代表用户已经授权上传 GitHub、公开数据、部署外网服务或删除历史文件。

当前最优先的任务不是增加 ZINB，而是修复数据、验证及复现问题。推荐顺序：

> 冻结证据与明确计数口径 → 修复空间数据和信息泄漏 → 建立新测试和可靠数据管道 → 公平模型比较与校准 → 月度预测接口 → 演示与运行监控 → 有证据再扩展模型。

### 工作目录

- **当前待优化项目**：`/Users/liumingyuan/Documents/project of stat38888_副本`
- 原课程项目：`/Users/liumingyuan/Documents/project of stat38888`，不是本次修改目标。
- 详细审计报告：`/Users/liumingyuan/Documents/stat3888-audit-2026-09-19/AUDIT_REPORT.md`
- 全文件清单：`/Users/liumingyuan/Documents/stat3888-audit-2026-09-19/file_inventory.csv`
- 审计基准 commit：`c5cc69f`。接手后先重新检查 HEAD 和工作区，勿假设未发生后续变化。

旧 `CODEX_HANDOFF.md` 是历史交接，其中“泄漏已完全修复”“系统性高估”“可以直接实用”等判断不能照搬。应以代码、可复现实验和本次审计证据逐项核对。发现证据冲突时，记录复核方法与结果，不盲从任一交接文档。

## 2. 已确认的基础与运行边界

### 可以保留的工作

- 新 `src/` 模块化结构和统一模型接口。
- 时间顺序切分、扩展窗口 CV、seasonal naive 基线。
- 每折拟合时间基函数和 context 的思路。
- 下载/转换脚本、结果表、可视化框架及课程起源说明。

### Codex 已做的验证

- 全量清点 1,058 个非 Git 元数据文件；其中 1,027 个与原课程目录字节一致。
- 完整阅读新版核心代码、README 和原交接文档；旧资料做清点、静态扫描与哈希比对，并非逐页/逐帧重新验收。
- 在隔离副本重跑月度流程及小时分析；**5 份结果 CSV 与项目已有结果逐字节一致**。
- 月度流程正常结束，但出现“50 or more warnings”；不能据此宣称所有模型拟合健康。
- 全目录 54 个 R 文件和 43 个 Python 文件语法解析通过；**语法解析不等于业务测试**。
- 审计运行环境是 R 4.6.1、glmnet 5.0；没有验证最低 R 4.2 兼容性，也没有完成全新机器的网络到结果验收。
- 审计没有改动项目代码、数据和既有产物；本 handoff 是随后新增的交接文件。

### 旧版本的可复现成绩——只作修复前基准

| 项目 | 数值 |
|---|---:|
| 2025 测试 MAE | 0.8327644498 |
| 2025 测试 RMSE | 1.9686174566 |
| 2025 测试 R² | 0.6651411800 |
| 选定 lambda | 0.02848035868435803 |
| 2025 实际合计 | 2,125 |
| 2025 预测合计 | 1,371.4498226 |
| 预测总量相对偏差 `(预测−实际)/实际` | −35.4612% |

修复后允许分数下降，不要为了维持 0.665 保留泄漏或错误数据。若数据口径或样本集合改变，应明确区分“修复前后同口径对照”与“新口径结果”，不能直接当作算法提升。

## 3. 第一阶段：必须先处理的问题

每项按“复现问题 → 增加能发现问题的测试 → 最小修复 → 验证 → 更新记录”推进。此阶段不新增复杂模型或外网部署。

### A. 未知区域 NSA 和错误空间坐标

**位置**：`scripts/convert_data.py`、`src/prepare_data.R`、`src/features.R`、`src/visualize.R`。

证据：所谓 141 个社区是 140 个历史社区加 `NSA`。原始表有 370 行 NSA；聚合坐标 `(0,0)` 被用于空间建模，并成为 RBF 中心之一。训练空间尺度由移除 NSA 后的约 0.1018712 变为 6.663885，放大约 65.4 倍。地图把真实社区压缩为一团。

实施要求：

- 明确未知区域分类，保留其数量审计，但不赋予假坐标或放入空间距离计算。
- 优先使用版本固定的官方 140 社区边界/代表点；如果改用 158 社区，必须说明历史映射与口径变化，不能直接混用。
- 若使用事件坐标代表社区，只能基于当前训练窗口计算；无历史信息时定义明确回退方式。
- 空间距离采用适当投影；中心、缩放、带宽的学习尊重训练边界。
- 不能只调整图片坐标范围来掩盖源数据问题。

验收：区域数量与字典一致；未知地点不参与距离计算；非法/缺失坐标有显式处理；地图可辨识社区；未知记录与建模记录总量可对账。

### B. 切分前全量坐标造成未来信息泄漏

**位置**：`src/prepare_data.R` 的 `make_monthly_panel()`。

证据：坐标中位数先由含 2025、2026 的全量事件算出，再切分。全量坐标与截至 2024 年末的坐标相比，99/141 个区域不同。仅改未来事件的坐标，会改变历史训练特征。每折重建 recipe 不能消除这一上游问题。

实施要求：把事件依赖的空间统计移入训练内拟合，或替换为事件无关的固定地理信息。区域类别集合不能靠未来事件发现。绘制事后描述图可使用全期数据，但不得把其统计量传回预测模型。

验收：固定预测起点，扰动未来标签、坐标、事件属性或增加未来记录，训练数据特征和拟合参数保持不变。适用于每个 CV fold，而不只是最终切分。

### C. 先确定预测什么，再决定重复处理和时间口径

**位置**：`scripts/download_data.py`、`scripts/convert_data.py`、`src/prepare_data.R`。

证据：原始 40,583 行对应 36,449 个不同 EVENT_UNIQUE_ID；当前导出字段下有 495 行完全重复。重复事件可能对应多辆车等业务含义，不能未经核实就删除。当前 `n()` 是按行计数，却称“报案数”。按 OCC_DATE 聚合的是发生月份，而不是 REPORT_DATE 报案月份；2025 年发生的记录中有 26 行在 2026 年才报案。

实施要求：

- 查阅官方字段说明，明确一行、一个案件、一辆自行车的关系，形成 `data_dictionary.md`。
- 保留 OBJECTID（源对象主键）、EVENT_UNIQUE_ID、OCC_DATE、REPORT_DATE、快照时间及必要地理编码。
- 下载端检查对象主键重复；不要把事件 ID 天然当唯一行主键。
- 定义预测目标：已报告的失窃自行车记录数、不同案件数或新报案量，并统一文档与指标命名。
- 定义预测起点时数据可用性和迟报处理。当前快照即便有 REPORT_DATE，也不能完全重建历史数据库的修订状态；无历史快照时披露回顾性评估限制。
- 数据口径暂无法确定时，可以继续工程修复，但不得给新模型结果套上未经核实的“案件数”定义。

验收：目标定义、聚合规则、重复规则、未知记录、日期范围都可审计；处理前后总量逐项对账；不将无记录、数据缺失、尚未完整更新的月份一律当成 0。

### D. 纠正误差解释，补总量与分层诊断

**位置**：`src/models.R`、`src/evaluate.R`、`README.md`。

证据：2025 预测合计低于实际约 35.5%；只有 1、2 月高估，其余 10 个月低估。旧 README 的“下降趋势导致系统性高估”与整体结果相反。

实施要求：

- 修正文案，不把“分布漂移是主要原因”当作已完成因果归因。
- 输出月度/全年总量误差、非零样本误差、活跃社区误差及社区分层指标。
- 直接 `expm1(E[log1p(Y)|X])` 不等于条件计数均值，需把回变换偏差作为候选原因验证，而非直接认定其解释全部低估。
- 校准参数只能来自训练数据或时间外验证预测；严禁用 2025 实际总量反推系数再评估同年。

验收：误差方向和数字由输出表自动生成；明确主要优化 MAE、RMSE 还是数量校准；同时报告改进与代价。

### E. 新测试、干净安装、下载错误处理

**位置**：`.gitignore`、`requirements.txt`、两个数据脚本、`src/run_pipeline.R`。

已复现的问题：

- 旧 tests 测试旧 `R/`，整个 `tests/` 又被忽略，新 `src/` 没有对应测试。
- 全新 checkout 没有 `data/raw/`，下载脚本未创建目录，会报 FileNotFoundError。
- JSON 返回 `error` 时，下载器可能当成零行成功保存；中途失败可能输出部分数据。
- `scan("requirements.txt", what="character")` 未指定注释处理，会把说明文字读成包名。
- 从其他工作目录用绝对路径启动主流程，仍因 `project_paths()` 没传已求出的 ROOT 而失败。

实施要求：

- 新建跟踪中的新版测试目录，避免覆盖旧测试；或精确修改忽略规则，使新测试纳入 Git。
- 下载创建目录，校验 API 错误/schema/总数/对象 ID，合理重试，成功完整校验后才原子替换结果。
- 固定数据快照并记录源 URL、更新时间、下载时间、哈希；不要静默覆盖原始基准数据。
- 用明确安装脚本或 R 原生依赖管理；若继续 scan，正确处理 `#` 注释。区分 base/recommended 与需安装的依赖。
- 锁定 R/包版本并说明支持范围；本机 Python 出现过证书校验失败，应修复证书环境，不能关闭 TLS 校验。
- 脚本从任意工作目录都指向明确的项目根；小时分析独立创建输出目录并检查依赖。

最低测试要求：

| 测试组 | 要覆盖的情形 |
|---|---|
| 下载 | 首次运行、错误 JSON、空响应、分页/对象 ID 重复、半途失败、目标已有好文件 |
| 数据 | schema 缺列、无效日期、NSA、零坐标、重复业务记录、迟报、范围外日期 |
| 验证 | 未来扰动不影响历史、训练/测试时间顺序、不同时间起点、短序列 |
| 模型 | NA/Inf、输出长度、新社区、收敛状态、常量目标指标 |
| 工程 | 任意 cwd、模型保存重载一致、干净 checkout 合成数据 smoke test |

CI 的基础测试应不依赖实时外网；真实数据端到端验证单独记录。

## 4. 第二阶段：重新建立可信实验

### A. 公平对照，而非替换一个模型名字

旧 Ridge 有社区 one-hot 和 context；Poisson/NB 没有，只有 Ridge 调参。NB 还去掉截距，并存在需调查的拟合警告。不能把这些结果解释成模型族优劣。

- 建立“特征组 × 模型族”的实验表。
- 保留 seasonal naive、历史均值等基线；增加最近窗口的季节均值作为实际可用基线。
- 用可识别、可稳定拟合的社区/季节/时间结构比较 Ridge、惩罚 Poisson、NB 或 GAM。
- 对无惩罚模型处理共线性，不要直接删除最重要的整组社区信号；为不同模型提供合理且可比的调参预算。
- 捕获每折的收敛状态、警告、耗时和失败原因。失败折不可悄悄删掉再平均。
- 全特征 Poisson 在 Codex 的一次诊断运行中约 4 分钟未返回，已终止且无可用分数。后续需设置合理资源上限、检查病态设计和惩罚路径，不能把此次终止写成模型失败的结论。

### B. 分清 context 的预测增益与解释性

相同 lambda 下删除 context，旧测试 R² 从 0.6651412 变为 0.6522785，约 0.013 的差异可复现。但空间 RBF/context 在社区内恒定，落在完整社区 one-hot 的列空间中；数值投影残差约 4.4e-14。增益可能来自 Ridge 的有效惩罚变化，不能宣称增加了独立可识别的驱动因素。

- 做多折消融，各候选分别在训练验证数据上调参。
- 明确 context 当前是月度占比均值（无事件月记 0），不是按事件加权的历史场所占比；选择定义并准确命名。
- 检查是否需要空间平滑、固定效应、层级部分汇聚，而不是同时堆入冗余列后解读系数。

### C. 验证协议要匹配用途

- 若产品是每月重训后预测下月，新增 horizon=1 的滚动起点回测。
- 保留原 horizon=12 实验时单独报告，不能混为同一任务。
- 修 `make_expanding_folds()` 的相对时间起点：输入 time_index 从 25 开始时，当前 initial_months=60 实际首折只有 36 个月。
- 修短序列边界：目前可出现 `wrong sign in 'by' argument`。
- 调参和模型选择只用训练验证；选中参数的 CV 最小值不是独立评估。
- 2025 已用于多次诊断和方案讨论，不再简单称“从未接触的测试集”。冻结方案后选择确实未参与决策的新时间窗，或采用嵌套时间评估。
- 2023 与 2025 不同年份的 R² 不能直接证明算法提升/退化。当前相对 seasonal naive 的旧测试 RMSE 改善约 5.84%，CV 平均 RMSE 改善约 1.01%，需要报告分折稳定性。

### D. ZINB、趋势和时空交互属于候选实验

- 零值比例高不直接证明零膨胀机制。先拟合合理的 NB/GAM，检查剩余零值、离散及残差时空相关，再决定 ZINB/hurdle。
- 训练边界内构造 B-spline 不保证边界外预测稳定；关闭 warn.outside 也不会消除外推问题。比较不同 horizon 和更稳定的趋势形式。
- 当前基本是共享时间/季节效应加社区常量；可比较有收缩的社区趋势或社区×季节交互。
- lag 特征是否有效取决于预测 horizon、可用时间和验证方式，不能因过去一次失败永久排除。
- 任何新方法都要满足同口径、同预测起点、同数据可用性、相同基线和诚实失败记录。

## 5. 第三阶段：实现可使用的月度预测

首个产品建议为**社区层面的下月数量预测和资源规划辅助**。不要一开始升级到网格×小时，也不要宣称已证明能提高巡逻效率。

### 推荐接口

下面是拟议接口，不是当前已存在的代码：

```text
fit_model(training_data, config) -> model_bundle
predict_future(model_bundle, forecast_origin, horizon, geography) -> forecast_table
evaluate_forecasts(forecasts, matured_actuals) -> evaluation_report
```

model_bundle 至少包含拟合对象、recipe、特征顺序、区域字典、校准/区间组件、配置、训练截止时间、数据哈希、依赖版本和模型版本。

forecast_table 至少包含：

```text
model_version, data_snapshot_id, forecast_origin,
target_month, horizon, neighborhood_id,
prediction, lower, upper, interval_level, baseline_prediction,
data_cutoff, geography_version, status
```

### 必须补上的行为

- 生成无真实标签的未来月份，预测函数不能要求未来 actual。
- 明确保存模型，并测试重载后的预测一致性。
- 新区域明确拒绝或采用已定义回退；当前未知社区会导致 model.matrix 丢行后 cbind 报错。
- 预测 NA/Inf 或长度不符立即失败；常量目标的 R² 标注不可定义，不能默默汇总 NaN。
- 预测区间在时间顺序的校准集学习，并核验不同季节、活跃程度社区的覆盖率；不得套用不满足假设的独立同分布保证。
- 数量指标之外，比较固定名额（如 10 个社区）的事件覆盖率/排名表现；使用相同预算的基线，不把回顾性排名直接当实际干预收益。
- 外部天气特征使用预测当时可得的预报或历史统计，不用未来实测天气冒充部署输入。

### 轻量演示

建议 R Shiny 或等价轻量页面，展示月份选择、社区地图、预测区间、重点社区列表、基线对照、数据新鲜度与方法限制。预计算输出即可，不要求页面每次交互重训。

先本地演示，再依据用户另行授权决定部署。更新频率匹配源数据，不把定期更新宣传成实时预测。记录漂移、迟报修订、预测误差和回退情况。

## 6. 文档与历史材料处理

- README 保留课程起源和个人贡献，明确 AI/协作辅助边界，不虚构独立完成程度。
- R² 不是准确率，不写“准确率 66.5%”。
- 旧小时图标题为 2014–2025，实际含 706 条 2026 记录；应统一过滤或动态标题。前三小时是 18、17、12 点，午夜为第五、属局部峰；先核实小时字段质量，再解释午夜现象。
- 描述性数量不是暴露风险率；没有停车/骑行暴露量就不宣称某地点个体风险更高，没有干预证据就不宣称巡逻更有效。
- 数据许可证按实际来源核实；代码许可与数据/课程材料许可分开。服务说明提及 Ontario、市门户列 Toronto，需记录选择依据。
- 原 `CODEX_HANDOFF.md` 中“CSV 全不跟踪”的检查不准确：结果表可以跟踪，原始数据是否分发应单独判断。
- 原始数据、课程 `R/ handbook/ deliverables/ docs/ tests/ tmp/ .worktrees/` 不批量删除。需要新 tests 时保留旧版本并明确隔离。
- **不要直接运行旧 tmp 生成脚本或操作复制来的 worktree**：多个脚本写死原课程项目路径；两份 `.worktrees/*/.git` 仍指向原目录的 Git 元数据，可能误改原项目。
- 当前 remote 为 `jry/toronto-bicycle-theft-prediction`。交接称账号有误，但本次未核验账号和 token 权限。发布前确认用户目标，不索要明文 token，不写入凭据。

## 7. 交付物与完成标准

各阶段提供简短结果记录，不必每一步停下来询问。遇到需要改变业务目标、公开内容或不确定数据授权的选择，说明阻塞项并继续不依赖该选择的工作。

建议交付：

- `data_dictionary.md`：计数、时间、地域、重复和迟报口径。
- 数据 manifest：源地址、快照/更新时间、哈希、行数、转换规则版本。
- 新版测试与 CI、可执行安装/运行说明、锁定依赖。
- 版本化实验目录：配置、数据标识、每折指标、警告/失败、预测、图表、运行环境。
- 修复前后及公平模型/消融报告，说明比较是否同口径。
- 未来预测接口、模型持久化、预测表、本地演示。
- 更新 README、模型限制说明及下一位接手者的交接记录。

### 最终验收清单

- [ ] 原项目与历史材料保持完整；所有修改仅在当前约定项目范围内。
- [ ] NSA/异常坐标不会污染空间特征；地图可用。
- [ ] 数据单位、地域版本、迟报和快照限制明确。
- [ ] 所有训练派生特征通过未来扰动不变性测试。
- [ ] 干净 checkout 按文档可运行；API 错误不会生成假成功/破坏好数据。
- [ ] 新实现有独立测试，且实际被 Git 跟踪和 CI 执行。
- [ ] 候选模型的特征与调参比较合理，收敛失败显式记录。
- [ ] 时间验证与实际预测 horizon 一致，评估不复用调参标签。
- [ ] 报告社区误差、总量偏差、分层表现和不确定性，不仅一个 R²。
- [ ] 可保存模型、生成未来预测、重载复现，未知区域有确定行为。
- [ ] README 的每个关键数字可追溯到同一版本的输出。
- [ ] 清楚区分本地演示、离线回测和真实部署；不虚构效果。

## 8. 基准数据快照（勿静默覆盖）

| 文件 | SHA-256 |
|---|---|
| `data/raw/bicycle_raw_latest.csv` | `dc736ecfb3d6358d5a9eaaa18a1270e210164df321c4f0e0849378ec5c18ef30` |
| `data/raw/bicycle.csv` | `1b43684f62f24eb0229f64f5c6599411f2732cffc4d8201c10f7a369d7390d5e` |
| `data/raw/bicycle_enhanced.csv` | `d48575bf5e1b0fd62a897ef2600e2280c71354bb9cd00fc764d47aa39a33bf49` |

这些数字属于本次审计快照，不保证今后官方下载内容相同。接手时重新核对，数据变化时记录新版本，不把差异自动视为程序错误。

## 9. WorkBuddy 开始工作时的第一轮操作

1. 确认当前目录、Git HEAD、未提交改动及本文件；阅读完整审计报告。
2. 保存基准 manifest 和修复前结果，不覆盖现有数据。
3. 建立新版测试入口，优先复现 NSA、未来坐标泄漏、下载错误和根目录解析问题。
4. 按第一阶段实施并验收；目标口径核实与独立工程修复可交错推进。
5. 输出第一阶段交付记录，再进入公平比较和月度预测接口开发。

**最终目标不是守住现有分数，而是交付一个结果可信、运行可复现、未来预测可检验、个人贡献可说明的项目。**
